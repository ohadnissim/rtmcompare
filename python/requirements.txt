# MED-20: exact versions matching the bundled python-bundle. Bump here AND rebuild
# all three bundles (arm64, intel, win) whenever a version changes.
# Last verified: 2026-05-13 against python-bundle/ dist-info entries.
librosa==0.11.0
numpy==2.4.4
pyloudnorm==0.2.0
scipy==1.17.1
torch==2.11.0
# demucs and visqol are not present in the bundle as dist-info; install-time deps only.
# Pin ranges to avoid silent regression until a bundle rebuild pins them tighter.
demucs>=4.0.0,<5.0.0
visqol>=0.1.0,<1.0.0
